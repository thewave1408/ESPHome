binary_sensor:
  - platform: status
    name: Online
    id: ink_ha_connected

sensor:
  - platform: internal_temperature
    name: "ESP Temperature"
    id: sys_esp_temperature
    update_interval: 10s

button:
  - platform: restart
    icon: mdi:power-cycle
    name: "ESP Reboot"
